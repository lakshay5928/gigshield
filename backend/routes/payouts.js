const router=require('express').Router(); const pool=require('../config/db'); const auth=require('../middleware/auth'); const{mockUpiPayout}=require('../mock/razorpayMock');
router.get('/my',auth,async(req,res,next)=>{try{const{rows}=await pool.query(`SELECT py.*,c.disruption_hours,a.alert_level,a.ward_id,a.started_at FROM payouts py JOIN claims c ON c.id=py.claim_id LEFT JOIN alerts a ON a.id=c.alert_id WHERE py.worker_id=$1 ORDER BY py.created_at DESC`,[req.worker.id]); res.json(rows);}catch(e){next(e);}});
router.get('/summary',auth,async(req,res,next)=>{try{const{rows:[r]}=await pool.query(`SELECT COUNT(*) AS total,COALESCE(SUM(amount),0) AS total_amount,COALESCE(AVG(amount),0) AS avg_amount,COALESCE(AVG(processing_ms),0) AS avg_ms,MAX(paid_at) AS last_payout FROM payouts WHERE worker_id=$1 AND status='paid'`,[req.worker.id]); res.json(r);}catch(e){next(e);}});
router.get('/all',async(req,res,next)=>{try{const{rows}=await pool.query(`SELECT py.*,w.name,w.mobile,w.city FROM payouts py JOIN workers w ON w.id=py.worker_id ORDER BY py.created_at DESC LIMIT 200`); res.json(rows);}catch(e){next(e);}});
router.post('/simulate',async(req,res,next)=>{try{const{claim_id,worker_id,amount}=req.body; if(!claim_id||!worker_id||!amount) return res.status(400).json({error:'claim_id,worker_id,amount required'}); const t0=Date.now(); const upiRef=await mockUpiPayout(amount,worker_id); const ms=Date.now()-t0; const{rows:[py]}=await pool.query("INSERT INTO payouts (claim_id,worker_id,amount,status,upi_ref,paid_at,processing_ms) VALUES ($1,$2,$3,'paid',$4,NOW(),$5) RETURNING *",[claim_id,worker_id,amount,upiRef,ms]); await pool.query("UPDATE claims SET status='paid' WHERE id=$1",[claim_id]); res.json({payout:py,upi_ref:upiRef,processing_ms:ms});}catch(e){next(e);}});
module.exports=router;
